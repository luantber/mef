from setuptools import setup

setup(
    name="mef",
    version="0.2.0",
    packages=["mef"],
    # install_requires=[
    #     'requests',
    #     'importlib-metadata; python_version == "3.8"',
    # ],
)
