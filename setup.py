from setuptools import setup

setup(
    name='mef',
    version='0.0.1',
    packages=['mef'],
    # install_requires=[
    #     'requests',
    #     'importlib-metadata; python_version == "3.8"',
    # ],
)
